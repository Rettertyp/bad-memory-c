#ifndef BAD_MEMORY_ALG_H
#define BAD_MEMORY_ALG_H

#include "graphNode.h"

bool badMemoryAlgorithm(IntervalSet* intervalSet);
IntervalSet* assign(IntervalSet* intervalSet, int groupSize);

#endif // BAD_MEMORY_ALG_H